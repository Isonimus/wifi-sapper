# Ledger

The single mutable file in this repo (stele:ADR-0001). Everything else is
immutable or generated. Open work, deferrals, and known defects all live here — there is
no second tracking file, because two files require manual sync and manual sync does not
happen.

**Closing an item means deleting its line here.** Do not annotate the source ADR; the
ADR's claim ("at the time of this decision we deferred X") stays true forever and needs
no update. Single writer, one direction.

Format: `- [type] description (ADR-NNNN)` — type is `bug` | `feature` | `deferred` |
`audit`. Cite the source ADR where one exists; rule 8 checks that the citation resolves.
A decision that lives in **another** repo is cited `<repo>:ADR-NNNN` — the linter cannot
open that corpus, so it skips qualified references (stele:ADR-0009).

## Open

<!-- One line per open item. Delete the line to close it; the git log is the done-record.
     Slices below are the initial roadmap; each earns its ADR(s) as it is picked up, and the
     ADR citation is added to its line then (rule 8 rejects a citation that does not resolve). -->

- [feature] serial control channel — STIMULUS half only: fault/stimulus injection behind SAPPER_TEST_HOOKS (synthetic handshake via the capture seam, stubbed wpa-sec responses, forced heap/time faults, hourly-sync clock advance), compiled out of release (ADR-0003). The observation half shipped in slice-0005; land the stimulus half with the engine it drives (slice-3/4)
- [deferred] flash-encryption / secure-boot for secrets at rest — plaintext NVS is the slice-2 baseline; the key is readable from a flash dump until this lands (ADR-0006)
- [feature] optional SD/LittleFS seed-NVS-on-first-boot for boards with storage — a power-user provisioning path alongside the portal (ADR-0006)
- [feature] physical re-provision trigger (button/keyboard-hold at boot) — needs an input HAL the repo does not have yet; slice-0007 shipped only the automatic STA-fail fallback into the portal, so this is the manual override (ADR-0006)
- [feature] capture: hop dwell hardware tuning — slice-0014's per-channel dwell defaults to a spec-grounded 300ms (≈3× the 102.4ms beacon interval), not yet a measurement; run verify:rf-discover on hardware to confirm/lower the reliable minimum and amend ADR-0013 with the measured number (ADR-0013)
- [feature] capture: RSSI-based AP/target ranking — needs widening the FrameConsumer seam to carry rx_ctrl metadata (RSSI), deliberately out of scope in slice-0014 to keep the seam to raw frame bytes (§4 invariant #8); add when target selection needs signal strength (ADR-0013)
- [feature] capture: deauth — pure deauth/disassoc frame building + raw-TX seam to force handshake renegotiation, gated by the SAPPER_TEST_HOOKS actuation rule; also owns the remaining on-air proof of a real (deauth-forced) 4-way handshake → wpa-sec-valid pcap, now that slice-0018 built the byte sink and proved the emit→store→upload path with a synthetic stimulus (ADR-0009, ADR-0003, ADR-0011, ADR-0017)
- [feature] capture: station scanner — passive client discovery for targeted deauth of connected clients (ADR-0009)
- [deferred] PMKID (clientless) capture — a second capture mode alongside the 4-way handshake, deferred to keep the capture core to one path; when built, confirm the wpa-sec response classification against real PMKID responses — the count-aware classifier (net/wpasec_response.cpp) already reads the count on *every* "written to 22000 hash file" line, so a "0 PMKID ... written" reply classifies Rejected (host-tested, test_wpasec_response), and the remaining work is verifying the real PMKID success wording on air (ADR-0009, ADR-0017)
- [feature] capture: hunt quiesce settle hardware tuning — slice-0016's phase-transition settle (before a sink reset/re-target) defaults to an architecture-derived ceiling over a microsecond memcpy callback, not yet a measurement; run verify:hunt on hardware to confirm/lower it from the observed phase timing and amend ADR-0015 (ADR-0015)
- [feature] slice-6: wpa-sec cracked-download + hourly scheduler + per-BSSID manifest + new-password alerts (shares the STA session the slice-0018 drain brings up, ADR-0017 decision #6) — picked up in slice-0020 (ADR-0019); host lanes green, remaining is the on-air Scenario J run on hardware
- [deferred] cracked manifest bound (kMaxCrackedEntries = 256) — an account with more than 256 cracked results churns its oldest entries every sync and can re-alert on the churn; eviction is by firstSeenCrackedMs, itself millis()-wrap-ambiguous on a >49-day run. 256 is an architecture-derived bound (~27KB manifest + ~27KB sync buffer, comparable to the upload path's pcap buffers), not a measurement: run the download probe against real accounts to size the true distribution, then either raise the bound or page the CrackedStore instead of holding it whole (ADR-0019 decision #4)
- [deferred] pinned-CA maintenance — the wpa-sec upload trusts one anchor, the self-signed GTS Root R4, which expires 2036-06-22; wpa-sec could migrate off Google Trust Services sooner. A TLS handshake failure after a period of success is the signal; the fix is re-measuring the live chain and updating the pinned PEM (src/net/wpasec_root_ca.h) in a firmware release (ADR-0017 decision #1)
- [feature] SD as an optional larger/overflow CaptureStore backing behind the same seam — internal-flash LittleFS is the portable floor (slice-0018); an SD-equipped board could hold a deeper offline queue, built when broadening hardware (ADR-0017 decision #3, slice-8)
- [deferred] wpa-sec response parsing over the *raw* HTTP stream — readAndClassify() (uploader_wpasec.cpp) hands the full response (status line + headers + any `Transfer-Encoding: chunked` framing) to classifyWpaSecResponse(), relying on strstr finding hcx's body tokens. It is fail-closed (a chunk boundary splitting a success token → Rejected → retry, never a false accept — the tokens occur only in hcx's body, not in headers/error pages) and works on the measured live traffic (MOVISTAR accepted); proper body extraction (strip headers, de-chunk) would remove the residual split-token false-reject. Deferred as device-only hardening — the pure classifier is already host-tested (ADR-0017 decision #2)
- [feature] slice-7: alert surfaces — the shared surface event bus (ADR-0021) + LED status surface shipped in slice-0022, the push-notification webhook (ntfy/Discord) in slice-0024 (ADR-0023), and the on-device status-HUD/cracked-toast display in slice-0026 (ADR-0025); remaining surface, an additional bus subscriber touching no engine code: web dashboard (ADR-0021, ADR-0001)
- [deferred] webhook TLS trusts the system root-CA bundle (ADR-0023 decision 5), so a self-hosted ntfy behind a *private* CA is unsupported until its root is in the bundle — a handshake failure after configuring such a host is the signal; the fix is embedding the private root (a custom bundle) or documenting a public-CA requirement (ADR-0023)
- [deferred] webhook on-air proof is per-target — Scenario K (slice-0024) exercises whichever URL the operator provisions; whichever of ntfy/Discord was not run on air is unverified against the live service's real response codes/body (mirrors the slice-0022 single-LED deferral) (ADR-0023)
- [deferred] webhook retry has no per-item backoff and never requests its own STA window — a persistently failing endpoint re-attempts every window (bounded, no spin), and a fresh crack rides the sync window that produced it while a retry waits for the next window the supervisor opens anyway; add a notifier-requested window or per-item backoff only if retry latency proves too high in the field (ADR-0023 decision 2)
- [deferred] LED single-LED path (`LedKind::Single`, `Esp32LedDriver`) is unexercised — the reference Cardputer ADV has an RGB WS2812, so the plain on/off branch (pinMode + digitalWrite) has no on-air proof; verify it when a single-LED board profile is added (ADR-0021, slice-8)
- [deferred] the drain→status mapping is duplicated: `LedStatusSurface::statusFromDrain` (→`LedStatus`) and `ScreenToastSurface::statusFromDrain` (→`ScreenStatus`) read a `DrainOutcome` identically. Left duplicated on purpose — this is the second reader, not the third (quality-bar rule of three); do NOT hoist into a shared status enum both surfaces depend on until a third reader appears (ADR-0025)
- [deferred] the view-model→renderer split (surface computes a value object, an abstract renderer draws it) is now used by the LED and the screen; a third such surface (the web dashboard) is the rule-of-three trigger to consider extracting a shared "surface renders a view-model" abstraction (ADR-0025)
- [deferred] screen HUD shows no capture-queue depth — the event bus carries no queue-depth fact and a surface must not reach into engine state (§4 invariant #13); add a bus fact carrying it (or a pull snapshot) if queue depth proves worth showing on the panel (ADR-0025)
- [deferred] `parseBssid` (the "aa:bb:cc:dd:ee:ff" -> 6 octets sscanf parser) is duplicated in `rf_sniff_probe.cpp` and `deauth_probe.cpp` — the second instance, left duplicated on purpose (quality-bar rule of three). Both are device-only bench-probe helpers; do NOT hoist into a shared probe-util header until a third probe needs it (ADR-0027)
- [deferred] display-init failure is logged `[FATAL]` in main.cpp but not acted on: `drawBringupFrame`/`present` and now the screen surface (slice-0026) still use the display after `d.begin()` returned false. Cosmetic today (LGFX guards a null-buffer sprite, no crash), but not fail-loud (§3). The headless-first fix is to skip all panel use on init failure (main + screen_probe + the `huntLoopBegin` display arg) while the hunt continues — a boot-robustness change, out of slice-0026's scope (adversarial review, slice-0026)
- [feature] slice-8: broaden hardware — CYD / M5Stick / bare-ESP32 board profiles + per-profile memory & palette tuning (for the M5StickC Plus2, reuse the on-device-tested board definition from the dragon-ball-radar repo rather than deriving one fresh)
- [feature] capture: deauth cadence hardware tuning — slice-0030's `HuntConfig.deauthIntervalMs` defaults to a conservative 2000ms (one broadcast burst, then a listen gap for the client to reassociate and complete the handshake), an architecture-derived value over the seconds-scale reassociation latency, not yet a measurement. Run `verify:deauth-loop` on hardware with a real client and tune the interval against the observed forced-capture rate, then amend ADR-0029 with the measured number (mirrors the dwell/settle tuning items) (ADR-0029)
- [feature] capture: per-BSSID deauth allow/deny scoping — slice-0030 arms deauth all-or-nothing (armed → deauth every discovered AP; the authorized-factory case). A mixed environment where some in-range networks must be spared needs a provisioned allow/deny list the engine consults before transmitting at `capturingBssid()`; layered atop the ADR-0029 arm toggle when such an environment appears (ADR-0029, ADR-0015)
- [deferred] the captive portal form (`kFormHtml`) is stateless — it never renders the stored `deauthEnabled` or `webhookUrl` back, so re-opening the portal and saving re-enters ALL fields: an unchecked deauth box disarms, a blank webhook disables push (both fail-safe, but silent). A dynamic form would fix the surprise, but naively rendering the stored `webhookUrl` into served HTML would expose that secret (topic/token) to anyone who opens the portal, so a fix must render the arm checkbox state without echoing secret fields. Deferred; the slice-0030 verify precondition documents the re-save behavior (adversarial review, slice-0030; ADR-0029, ADR-0023)
- [defect] capture-notification suppressed when a landed capture reports StoreError — `UploadSupervisor::onCaptureReady` publishes `HandshakeCaptured` only on `OfferResult::Stored/Replaced/Evicted`, but `CaptureQueue::offer` returns `StoreError` even when the just-captured bytes DID land on flash and a later reconcile-stage `store_.remove()` (dedup/eviction of *other* entries) failed. That capture uploads normally, yet its banner/flash/`[CAPTURE]` line is suppressed — the exact "went unnoticed" symptom ADR-0031 fixes, for a capture that was not actually dropped. Cosmetic-only (§4 #13) and rare (needs a flash-remove failure); the fix needs a finer `OfferResult` that distinguishes "primary write failed" from "landed but housekeeping failed" (ADR-0017 territory), so deferred rather than coarsened here. Found by the slice-0032 adversarial pass (ADR-0031, ADR-0017)
- [feature] live hunt HUD on the panel — show the network currently being sniffed (SSID/BSSID) with Beacon/M1–M4 markers and a capture-progress bar; reference `../adversary/src/ui/screens/handshake_screen.cpp` (`drawIndicator` per message + the `progress_bar` component). Fed by a PULL SNAPSHOT the screen polls from the engine each tick (current target + its collected handshake-message set), NOT a bus event — §4 invariant #13 blesses the pull snapshot the on-air verifies already use. Extends ADR-0025; needs a `HuntEngine` getter exposing the in-flight `CapturedHandshake`'s collected messages (ADR-0025, ADR-0015)
- [feature] webhook-on-capture + per-notification-type selector — extend the push webhook (ADR-0023) to also fire on a fresh capture (PSK-free: §4 invariant #14 forbids a plaintext PSK in any webhook payload, so a capture push carries ESSID/BSSID only), and gate each notification type (captured / cracked / sync-error) behind a provisioning-stored enable-set the surfaces consult. Checkboxes in the captive portal now (NVS-backed, same pattern as the ADR-0029 deauth arm toggle), rendered richly by the future web dashboard later. The capture bus fact it depends on now exists (`AppEventType::HandshakeCaptured`, ADR-0031) (ADR-0023, ADR-0006, ADR-0021, ADR-0031)
- [feature] wire cloud CI to run ADR-0004 lanes 1–2 on push (npm run test:native + build:boards) — backs §4 invariant 5 that no CI lane depends on attached hardware; device verify (lane 3) stays a hardware-runner/manual step (ADR-0004)
- [feature] expand README.md — it now carries the build/verify commands and the first-boot provisioning guide (slice-0007); still to add are the serial observation protocol reference and per-board bring-up/flashing steps (stele:ADR-0010)
- [deferred] OTA firmware update for field appliances — scope-heavy, revisit after the capture→sync spine (slices 1–6) is green

## Resolved

Entries move out of "Open" by deletion. A narrative of what merely *happened* belongs in
the git log; what belongs on an open item is evidence it cannot be re-derived later — a
measured result, a reproduction, a count the eventual decision turns on. Keep that with
the item while it is open, and move it into the ADR that closes it. What this file is not
is a record of completed work.
